[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
flask = "*"
requests = "*"

[scripts]
app = "python main.py"
test = "python test.py -v --failfast"

[requires]
python_version = "3.7"
